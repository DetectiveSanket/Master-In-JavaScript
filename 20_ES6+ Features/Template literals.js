

/* 
    Template literals, also known as template strings, are a feature in JavaScript that allows for easier string interpolation and multi-line strings. They are enclosed by backticks (`) instead of single or double quotes.

    Template literals provide several benefits:
        - String Interpolation: You can embed expressions inside a template literal using the ${} syntax. This allows you to easily include variables and expressions within a string without needing to concatenate them.
        - Multi-line Strings: Template literals can span multiple lines without the need for escape characters. This makes it easier to create strings that contain line breaks or formatted text.
        - Improved Readability: Template literals can enhance the readability of your code by allowing you to write more natural and expressive strings, especially when dealing with complex string constructions.
*/