
## All key concept of String in JavaScript at one place

1) String is immutable in JavaScript, which means that once a string is created, it cannot be changed. Any operation that modifies a string will create a new string.